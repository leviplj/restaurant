stages:
  - stage: development
    displayName: 'Deploy (DEV)'
    dependsOn: buildDev
    variables:
      - name: Project.Tag
        value: DEV
    jobs:
      - download: current
        artifact: manifests
      - template: deploy.jobs.yaml
        parameters:
          azdsEnabled: true
          environment: levipljrestaurant
          resource: default
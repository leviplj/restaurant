stages:
  - stage: development
    displayName: 'Deploy (DEV)'
    dependsOn: buildDev
    variables:
      - name: Project.Tag
        value: DEV
    jobs:
      - template: deploy.jobs.yaml
        parameters:
          azdsEnabled: true
          environment: levipljrestaurant
          resource: default
apiVersion : apps/v1beta1
kind: Deployment
metadata:
  name: restaurant 
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: restaurant 
    spec:
      containers:
        - name: restaurant 
          image: levipljacr.azurecr.io/restaurant
          ports:
          - containerPort: 8000